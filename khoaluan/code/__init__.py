"""v2 code — task-decomposed + reasoning + weighted-CE cho PULSE-7B.

Hiện thực mục 3–5 của ../method-spec.md. KHÔNG đụng source/LLaVA (core PULSE).
KHÔNG train trong milestone này — chỉ build dataset + loss tham chiếu + smoke test.
"""
